import styled from 'styled-components';
import { Flex } from '../../components';

export const RegistrationOption = styled(Flex)`
  justify-content: space-between;
  padding: 4px;
  margin: 4px 0;
  border-bottom: 1px solid #eee;

  p {
    font-weight: bold;
    min-width: 90px;
    margin: 0;
    padding: 0;
  }
`;
